
var util = require('util');
var util = require('util');

var   util = require('util');
var   sys   =     require ( 's'+'y'+'s' );
var system = '2';
var SySTEMBLARG = '2';
util.puts("sys should be replaced with util, but not in this output or "+system);
util.puts("1 ");
util.puts("2");
util.puts("3");

getNewFile(function (err, file) { if (err) { throw err; } /* do something with file */ });