
var sys = require("sys");
var sys = require('sys');

var   sys   =     require ( 'sys' );

var system = '2';
var SySTEMBLARG = '2';
sys.puts("sys should be replaced with util, but not in this output or "+system);
sys.puts("1 ");
sys.puts("2");
sys.puts("3");

getNewFile(function (err, file) { if (err) { throw err; } /* do something with file */ });